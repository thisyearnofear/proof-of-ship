name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile --filter ./frontend
      - name: Lint
        run: pnpm --filter frontend run lint
      - name: TypeScript
        run: pnpm --filter frontend exec npx tsc --noEmit
      - name: Test
        run: pnpm --filter frontend run test -- --run --reporter=verbose
      - name: Security Audit
        run: pnpm audit --audit-level=high || true
      - name: Build
        run: pnpm --filter frontend run build
        env:
          ALLOW_DEMO_PAYMENTS: 'true'
          NEXT_PUBLIC_SOLANA_CLUSTER: 'devnet'
          NEXT_PUBLIC_SOLANA_PROGRAM_ID: '14uLETygxjh89fHFwYUaRRhHE9E9XrYcSh6SsF8SEw1K'
